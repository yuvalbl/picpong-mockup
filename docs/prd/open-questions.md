# Open questions — mockup-v4 / v5

> Running list of decisions still pending Yuval/Kuki input. **From now on, open questions live here** (not scattered in the individual PRDs). Includes the live Meeting-2 blockers (D4/D5/D6 + Q-C/D/E/G) whose working home is `../todo/README.md` — mirrored here so this stays the one canonical open-state list. When one is resolved, move the decision into the relevant PRD's "Decisions" section and mark it resolved here with the date + answer.

| # | Question | Raised in | Owner | Status |
|---|---|---|---|---|
| OQ-1 | **Form contact requirement:** is "**at least one of Email/Phone** required" (current PRD 2 §4.1 spec) the right rule — or should we force **both**, or make **Email the sole required** channel? Lowest-friction is at-least-one; forcing both raises friction but guarantees two reply paths; Email-only is simplest but loses Kuki's phone/WhatsApp lead. | PRD 2 §4.1 | Yuval + Kuki | **Open (2026-07-05: D1 skipped for now, owner decision - both surfaces keep their current, distinct rules - drawer: name + email-or-phone; inline `#contact`: name + email - each now visibly states its own rule per a11y-M3; unify when Kuki decides)** |
| OQ-2 | **WhatsApp number:** the prefilled `wa.me` link now points at the **Twilio WhatsApp sandbox** (`WA_NUMBER = "14155238886"`, +1 415 523 8886) for testing. Still need Picpong's **real WhatsApp Business number** for launch. | PRD 2 §9 | Yuval + Kuki | **Open (sandbox set; 2026-07-05: owner decision to keep the sandbox for now, now also editable in the backoffice mockup's Admin settings screen)** |
| OQ-4 | **Taxonomy wording (final):** the English category labels — Meeting-2 Q-B, tracked as blocker **D4** in `../todo/README.md`. Still pending Kuki. `mockup-v5` ships a provisional set (Events / Conventions / Trade-show booths). | Meeting 2 Q-B / todo D4 | Kuki | **Open (2026-07-05: A2 stays as-built pending Kuki's confirmation of final wording)** |
| OQ-5 | **Lead audience segment:** which audience leads the messaging — Meeting-2 Q-A, blocker **D5** in `../todo/README.md`. | Meeting 2 Q-A / todo D5 | Yuval + Kuki | **Open** |
| OQ-6 | **Sustainability messaging intensity:** how loud the eco/"sustainability must scream" framing gets — Meeting-2 Q-F, blocker **D6** in `../todo/README.md`. A1/A2 in v5 are implemented pending this. | Meeting 2 Q-F / todo D6 | Yuval + Kuki | **Open (2026-07-05: A1 stays as-built pending Kuki's confirmation of intensity)** |
| OQ-7 | **Client-logo assets + roster mismatch:** the 2026-07-05 pass found the 8 SVGs in `mockup-v5/assets/brand/logos/` are garbled fakes - wrong letterforms on 7 of 8 (e.g. `wix.svg` renders "VIX", `amdocs.svg` is an unrelated Mastercard-style mark); only `landa.svg` is a correct logo. Wiring them was reverted - shipping fake versions of real companies' logos is a brand risk. Separately discovered: the home logo-marquee roster (Microsoft, Mifold, Electra, Clalit, HOT, Brinks, Azrieli Malls, Pashut Tari) and every footer's "Trusted by" roster (Microsoft, Intel, Google, IBM, Landa, Amdocs, Wix, Maccabi) are two different, unrelated client lists appearing on the same pages. Need Kuki to confirm the canonical client roster and supply real logo SVGs before this can ship - carries todo **C1**. | Found during 2026-07-05 Phase 4 pass / todo C1 | Kuki | **Open (raised 2026-07-05)** |
| — | *Also live from Meeting 2, tracked in `../todo/README.md` (not yet PRD-numbered): Q-C research-vs-fast-track, Q-D design rounds, Q-E photo delivery, Q-G WhatsApp single-image long-term.* | Meeting 2 | Yuval + Kuki | **Open** |
| OQ-3 | **Home page has THREE "our work / our clients" surfaces — do we need all of them, specifically the journal strip vs the projects teaser?** The three (top→bottom in `index.html`): **(a) Client-logo marquee** — eyebrow "Our clients include", `~L238`, scrolling logos; **(b) Studio-journal strip** — heading "From the studio journal", `.latest-strip` `~L269`, the daily-feed teaser → `latest.html`; **(c) Projects teaser** ("Selected work") — heading "Trusted on the world's biggest show floors", `.grid-projects--feature` `~L480`, featured project tiles → `project-detail.html`. The open question is **(b) vs (c)**: different intended jobs — **(c)** = curated, evergreen **portfolio/proof** linking to deep case studies; **(b)** = a rolling, frequently-updated **"we're alive, here's recent work"** feed (freshness + SEO + reason to return). Risk: they *look* alike (same brands, same hero shots) so a visitor reads "projects… and more projects." Decision: keep both (and differentiate **(b)** into a real *diary* register — process/behind-the-scenes/dated, not a second polished portfolio), or cut/merge. (a) the logo marquee serves a distinct third-party-trust job and is cheap — not the redundancy. **Assistant's recommendation:** **move the projects teaser (c) off Home and onto the Projects page** (which already hosts the full portfolio collage), leaving Home with **trust (a) + pulse (b)** — Home stops repeating the portfolio, and the deep case-study grid lives where someone browsing work expects it. Keep the journal strip (b) on Home but make it a genuine *diary* (process/dated), visually distinct from the portfolio so it never reads as "projects again." | PRD 3 §8 | Yuval + Kuki | **Open (2026-07-05: D3 owner decision - leave the home layout as-is for now, for Kuki to review)** |

## Resolved

_(none yet — move answered questions here with date + decision, and update the source PRD.)_

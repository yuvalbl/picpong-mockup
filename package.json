{
  "name": "picpong-mockup",
  "version": "0.1.0",
  "private": true,
  "description": "Picpong redesign — static HTML/CSS/JS mockups (cardboard-monograph direction) for client review.",
  "scripts": {
    "serve": "firebase emulators:start --only hosting",
    "deploy": "firebase deploy --only hosting",
    "deploy:preview": "firebase hosting:channel:deploy preview"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
